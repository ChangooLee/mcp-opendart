# OpenDART API 키 설정
# API 키는 OpenDART 웹사이트(https://opendart.fss.or.kr/) 에서 발급받을 수 있습니다.
# 회원가입 후 발급된 인증키를 아래에 입력하세요.
OPENDART_API_KEY=your_api_key_here

# 서버 설정
HOST=0.0.0.0  # 모든 IP에서 접근 가능
PORT=8000     # 서버 포트 번호
TRANSPORT=stdio

# 로깅 설정
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL 중 선택

# MCP 설정
MCP_SERVER_NAME=mcp-opendart

# 캐싱 설정 (선택사항)
ENABLE_CACHE=true
CACHE_EXPIRY=3600  # 캐시 만료 시간(초)